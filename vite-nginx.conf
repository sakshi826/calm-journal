server {
    listen 80;

    location /calm-journal {
        alias /usr/share/nginx/html/calm-journal;
        index index.html index.htm;
        try_files $uri $uri/ /calm-journal/index.html;
    }

    location = / {
        return 301 /calm-journal/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}